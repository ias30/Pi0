==================================================================================
                    OFFLINE_INFERENCE.PY 数据流
==================================================================================

1️⃣ 原始数据加载（从LeRobot数据集）
   ┌─────────────────────────────────────────┐
   │ 图像：CHW format, uint8 [0, 255]       │  ✅ 原始格式
   │ 状态：joint positions (radians)        │
   │ 动作：joint positions (radians)        │
   └─────────────────────────────────────────┘
                    ↓
2️⃣ Input Transform Pipeline
   ┌─────────────────────────────────────────┐
   │ repack_transforms.inputs                │
   │ data_transforms.inputs                  │
   │ Normalize(norm_stats)  ← 图像归一化     │  ✅ 只归一化一次
   │ model_transforms.inputs                 │
   └─────────────────────────────────────────┘
                    ↓
3️⃣ 转换为JAX数组 + Batch维度
   ┌─────────────────────────────────────────┐
   │ 图像：CHW, float32, 正确归一化          │
   │ 状态：归一化后的状态                    │
   └─────────────────────────────────────────┘
                    ↓
4️⃣ 模型输入
   └─→ model.sample_actions()


==================================================================================
                    REALMAN_INFERENCE.PY 数据流（当前版本 - 有问题❌）
==================================================================================

1️⃣ 原始数据获取（实时硬件）
   ┌─────────────────────────────────────────┐
   │ 图像：BGR, HWC, uint8 [0, 255]         │  ✅ 原始格式
   │ 状态：joint angles (degrees)           │
   │      ↓ 转换为radians                   │
   │ 状态：joint positions (radians)        │
   └─────────────────────────────────────────┘
                    ↓
2️⃣ preprocess_image() 函数  ⚠️ 问题所在
   ┌─────────────────────────────────────────┐
   │ BGR → RGB                               │
   │ Resize with padding                     │
   │ HWC → CHW                               │
   │ (img / 127.5) - 1.0  ← 手动归一化到[-1,1]│  ❌ 第一次归一化！
   └─────────────────────────────────────────┘
                    ↓
3️⃣ Input Transform Pipeline
   ┌─────────────────────────────────────────┐
   │ repack_transforms.inputs                │
   │ data_transforms.inputs                  │
   │ Normalize(norm_stats)  ← 再次归一化!    │  ❌ 第二次归一化！
   │ model_transforms.inputs                 │
   └─────────────────────────────────────────┘
                    ↓                           ⚠️ 图像被归一化了两次！
4️⃣ 转换为JAX数组 + Batch维度                   导致输入分布严重偏移
   ┌─────────────────────────────────────────┐
   │ 图像：CHW, float32, ❌双重归一化        │  ❌ 错误的数值范围
   │ 状态：归一化后的状态                    │
   └─────────────────────────────────────────┘
                    ↓
5️⃣ 模型输入
   └─→ model.sample_actions()  ← ❌ 输入错误导致输出错误


==================================================================================
                    关键差异标记 ⚠️
==================================================================================

❌ 差异点：realman_inference.py 在 preprocess_image() 中手动归一化图像到 [-1, 1]
           然后 transform pipeline 又用 norm_stats 再次归一化

✅ 正确做法：应该像 offline_inference.py 一样，只在 transform pipeline 中归一化一次

📊 数值影响：
   - 正确输入范围（offline）：经过一次 Normalize(norm_stats)
   - 错误输入范围（realtime）：先 [-1,1]，再经过 Normalize → 严重偏移
   
   举例：假设某像素原始值为 128（中灰）
   ✅ 离线：128 → Normalize(norm_stats) → 正确的归一化值
   ❌ 实时：128 → (128/127.5)-1=0.004 → Normalize(norm_stats) → 错误的归一化值

==================================================================================










==================================================================================
修复前（错误） ❌
==================================================================================
相机图像 [uint8, 0-255, BGR-HWC]
    ↓
preprocess_image()
    - BGR → RGB
    - Resize + Padding  
    - HWC → CHW
    - (img/127.5)-1 ← ❌ 第一次归一化到 [-1,1]
    ↓
Transform Pipeline  
    - Normalize(norm_stats) ← ❌ 第二次归一化！
    ↓
模型输入 [❌ 错误的数值分布，严重偏移]
    ↓
模型输出 [❌ 错误的S轨迹]


==================================================================================
修复后（正确） ✅
==================================================================================
相机图像 [uint8, 0-255, BGR-HWC]
    ↓
preprocess_image()
    - BGR → RGB
    - Resize + Padding
    - HWC → CHW
    - ✅ 保持 uint8 [0-255]，不做归一化
    ↓
Transform Pipeline
    - Normalize(norm_stats) ← ✅ 唯一的归一化点
    ↓
模型输入 [✅ 正确的数值分布，与训练数据一致]
    ↓
模型输出 [✅ 正确的S轨迹]